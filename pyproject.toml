[project]
name = "tablero-soporte-firefly"
version = "0.1.0"
description = "Tablero ejecutivo de soporte (Odoo helpdesk.ticket) para Firefly."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.60.0",
    "pandas>=2.2",
    "plotly>=5.24",
]

[tool.uv]
package = false
